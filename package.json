{
  "name": "empty-spaces",
  "version": "0.1.4",
  "description": "Checking how much space needs to be cleared given folder and maximum size allowed (fixed or percent out of hard-disk)",
  "main": "checkFreeSpace.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "test": "mocha",
    "coverage": "istanbul cover _mocha -- -R spec",
    "coveralls": "cat ./coverage/lcov.info | coveralls"
  },
  "author": "thehobbit85",
  "license": "MIT",
  "keywords": [
    "file system",
    "filesystem",
    "folder",
    "size",
    "file managment",
    "folder managment"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/Colored-Coins/Empty-Spaces.git"
  },
  "dependencies": {
    "async": "^0.9.0",
    "diskspace": "^0.1.7",
    "get-folder-size": "^0.1.1"
  },
  "devDependencies": {
    "jsonfile": "^2.0.1"
  }
}
